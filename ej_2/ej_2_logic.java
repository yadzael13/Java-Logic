package ej_2;
public class ej_2_logic {
    /**
     * De cuatro elementos dados como enteros, determinar si la figura formada por esos valoires
     * sería un rectangulo, un cuadrado o una figura irregular
     * 
     * *Ej.1
     * de los elementos = 50, 50, 23, 23
     *      se podria formar un "Rectángulo"
     * * Ej.2
     * de los elementos = 50, 50, 50, 50
     *      se podria formar un "Cuadrado"
     * * Ej.3
     * de los elementos = 50, 10, 21, 100
     *      se podria formar un "Irregular"
     * @param a1 --
     * @param a2 --
     * @param a3 --
     * @param a4 --
     * @return Si pertenece a un rectangulo, cuadrado o irregular
     */
    public static String exercise2Logic(int a1, int a2, int a3, int a4){
       //Logica
    }
}
